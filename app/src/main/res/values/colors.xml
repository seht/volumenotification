<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="launcher_background_color">@android:color/white</color>

    <!-- android:color/background_material_dark = #212121 (private API) -->
    <item name="theme_material_background_color" type="color">#212121</item>
    <!-- android:color/foreground_material_dark = #ffffff (private API) -->
    <item name="theme_material_icon_color" type="color">#ffffff</item>

    <!-- android:color/background_material_light = #fafafa (private API) -->
    <item name="theme_material_light_background_color" type="color">#fafafa</item>
    <!-- android:color/foreground_material_light = #de000000 (private API) -->
    <item name="theme_material_light_icon_color" type="color">#de000000</item>

    <!-- android:color/background_holo_dark = #000000 (private API) -->
    <item name="theme_holo_background_color" type="color">#000000</item>
    <!-- android:color/foreground_holo_dark = #ffffff (private API) -->
    <item name="theme_holo_icon_color" type="color">#ffffff</item>

    <!-- android:color/background_holo_light = #ffffff (private API) -->
    <item name="theme_holo_light_background_color" type="color">#ffffff</item>
    <!-- android:color/foreground_holo_light = #000000 (private API) -->
    <item name="theme_holo_light_icon_color" type="color">#000000</item>

</resources>
